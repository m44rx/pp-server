
server:
  port: 8081

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://gitlab.com/m44rx/py-microserv-tuto
          search-paths:
          - config-data